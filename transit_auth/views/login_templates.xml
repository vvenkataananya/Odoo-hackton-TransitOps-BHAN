<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <template id="login" inherit_id="web.login" name="TransitOps Login">
        <xpath expr="//form[contains(@t-attf-class, 'oe_login_form')]//div[contains(@class, 'field-login')]" position="before">
            <div class="mb-3 to-role-section">
                <label class="form-label" for="role_selected">Select Role</label>
                <div class="to-role-select-wrapper">
                    <select name="role_selected" id="role_selected" class="form-control">
                        <option value="fleet_manager">Fleet Manager</option>
                        <option value="driver">Driver</option>
                        <option value="safety_officer">Safety Officer</option>
                        <option value="financial_analyst">Financial Analyst</option>
                    </select>
                </div>
            </div>
        </xpath>
    </template>
</odoo>
